version: '2'
services:
  interfaceadaptativa-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/InterfaceAdaptativa/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=InterfaceAdaptativa
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
